import { styled } from '@mui/material';
import TableCell from '@mui/material/TableCell';

const FarmingStyledCellHead = styled(TableCell)(() =>
  ({
    backgroundColor: '#e5e5e5',
    color: 'black',
    padding: '0px',
    fontWeight: 'bold'
  })
);

export default FarmingStyledCellHead;